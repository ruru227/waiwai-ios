workflows:
  waiwai-testflight:
    name: waiwai - TestFlight
    instance_type: mac_mini_m2
    max_build_duration: 60
    environment:
      groups:
        - appstore_credentials
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.novalink.waiwai
      vars:
        BUNDLE_ID: "com.novalink.waiwai"
        XCODE_PROJECT: "waiwai.xcodeproj"
        XCODE_SCHEME: "waiwai"
      xcode: latest
    scripts:
      - name: Install XcodeGen
        script: |
          brew install xcodegen imagemagick
          ICON_DIR="$CM_BUILD_DIR/iOSApp/Resources/Assets.xcassets/AppIcon.appiconset"
          SOURCE="$CM_BUILD_DIR/iOSApp/Resources/AppIconSource.svg"
          magick "$SOURCE" -resize 1024x1024 "$ICON_DIR/AppIcon-1024.png"
          magick "$SOURCE" -resize 120x120 "$ICON_DIR/AppIcon-60@2x.png"
          magick "$SOURCE" -resize 180x180 "$ICON_DIR/AppIcon-60@3x.png"
      - name: Generate Xcode project
        script: |
          cd "$CM_BUILD_DIR"
          xcodegen generate
          xcodebuild -project "$XCODE_PROJECT" -scheme "$XCODE_SCHEME" -showBuildSettings > /dev/null
      - name: Apply Codemagic signing profiles
        script: |
          cd "$CM_BUILD_DIR"
          xcode-project use-profiles
      - name: Set build number
        script: |
          cd "$CM_BUILD_DIR"
          agvtool new-version -all "$PROJECT_BUILD_NUMBER"
      - name: Build signed IPA
        script: |
          cd "$CM_BUILD_DIR"
          xcode-project build-ipa --project "$XCODE_PROJECT" --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.app
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM
    publishing:
      app_store_connect:
        api_key: $APP_STORE_CONNECT_PRIVATE_KEY
        key_id: $APP_STORE_CONNECT_KEY_IDENTIFIER
        issuer_id: $APP_STORE_CONNECT_ISSUER_ID
        submit_to_testflight: false
        submit_to_app_store: false
